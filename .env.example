# Path to the SQLite database file.
# Locally (outside Docker) this defaults to weather.db in the working directory.
# docker-compose.yml overrides this to /data/weather.db so the database lives
# in the db_data named volume and persists across container restarts.
DB_PATH=weather.db
